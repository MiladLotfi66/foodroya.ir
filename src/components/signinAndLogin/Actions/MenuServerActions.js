// serverActions/menuActions.js
"use server";
export const ProductEditItem = async (id) => {
    // کد مربوط به ویرایش آیتم
    console.log(`Editing item with id: ${id}`);
  };
  
  export const ProductDeleteItem = async (id) => {
    // کد مربوط به حذف آیتم
    console.log(`Deleting item with id: ${id}`);
  };
  
  export const ProductSendItem = async (id) => {
    // کد مربوط به ارسال آیتم
    console.log(`Sending item with id: ${id}`);
  };
  